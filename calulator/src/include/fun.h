#define is_op(ch) ch == '+' || ch =='-' || ch == '(' || ch ==')' || ch == '*' || ch == '/' || ch =='#'
char compare(char a, char b);
double op(double a, double b ,char c);
void help();
void shoutdown(void);
